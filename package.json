{
  "name": "redi-cin7-stripe-integration",
  "version": "0.1.0",
  "private": true,
  "description": "Service to generate Stripe payment links and post completed payments to Cin7 Core.",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test:cin7": "ts-node src/test/test-cin7.ts",
    "test:stripe": "ts-node src/test/test-stripe.ts",
    "test:db": "ts-node src/test/test-database.ts",
    "test:worker": "ts-node src/test/test-worker.ts",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "axios": "^1.6.7",
    "better-sqlite3": "^12.5.0",
    "dotenv": "^16.4.0",
    "express": "^4.18.2",
    "pino": "^8.15.0",
    "stripe": "^14.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "prettier": "^3.1.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
