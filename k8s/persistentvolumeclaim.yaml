apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cin7-stripe-data
  namespace: payments
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard  # Adjust based on your cluster (gp2, gp3, etc.)
